#read csv file and put it in variable data
data <- read.csv("retrieval-practice-experiment.csv")
#Barplot the colum woonplaats with names given to each colum and put horizontal
barplot(table(data$woonplaats), horiz = TRUE, names.arg = c("Geen antwoord","Anders", "Dorp", "Platteland", "Stad"))
data <- read.csv("retrieval-practice-experiment.csv")
barplot(table(data$woonplaats), horiz = TRUE, names.arg = c("Geen antwoord","Anders", "Dorp", "Platteland", "Stad"))
barplot(data$rt)
plot(data$rt)
barplot(table(data$woonplaats), horiz = TRUE, names.arg = c("Geen antwoord","Anders", "Dorp", "Platteland", "Stad"), with(data$rt == "Ja"))
barplot(table(data$woonplaats), horiz = TRUE, with(expr = data$rt="Ja"))
table(data$woo)
table(data$woonplaats, with(data$rt, expr = data$rt = "Ja"))
table(data$woonplaats, with(data$rt, expr = data$rt == "Ja"))
table(data$woonplaats, with(data$rt, expr = data$rt = "Ja"))
